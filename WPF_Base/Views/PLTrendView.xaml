<UserControl x:Class="EPIHelper.Views.PLTrendView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
             xmlns:local="clr-namespace:EPIHelper.Views"
             mc:Ignorable="d" 
             d:DesignHeight="850" d:DesignWidth="1400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="200"/>
            <RowDefinition Height="3"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="3"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <GridSplitter Background="{StaticResource COLOR_ORANGE}" Grid.Row="1" HorizontalAlignment="Stretch"/>
        <ScottPlot:WpfPlot Grid.Row="2" x:Name="WpfPlot1" MouseMove="WpfPlot1_MouseMove"/>
        <GridSplitter Background="{StaticResource COLOR_ORANGE}" Grid.Row="3" HorizontalAlignment="Stretch"/>
    </Grid>
</UserControl>
